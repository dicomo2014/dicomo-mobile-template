[{% for event in site.categories.session %}
{title: "{{ event.title }}", id: "{{ event.id }}", start: "{{ event.start }}", end: "{{ event.end }}"}{% unless forloop.last %},{% endunless %}
{% endfor %}]
